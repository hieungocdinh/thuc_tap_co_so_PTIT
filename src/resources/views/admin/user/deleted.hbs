<div class="admin__menu">
    <div class="admin__menu-header">
        <h2>Tài khoản đã xóa</h2>
        <div class="">
            <button class="admin__menu-header-btn">
            <a href="/admin/user">Quay lại</a>
            </button>
        </div>
    </div>
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">STT</th>
                <th scope="col">Tên tài khoản</th>
                <th scope="col">Mật khẩu</th>
                <th scope="col">Loại tài khoản</th>
                <th scope="col">Email</th>
                <th scope="col">Số điện thoại</th>
                <th scope="col" colspan="4">Họ và tên</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
                <tr>
                    <th scope="row">{{sum @index 1}}</th>
                    <td class="admin__menu-item">{{this.username}}</td>
                    <td class="admin__menu-item">{{this.password}}</td>
                    <td class="admin__menu-item">{{this.type}}</td>
                    <td class="admin__menu-item">{{this.email}}</td>
                    <td class="admin__menu-item">{{this.phoneNum}}</td>
                    <td class="admin__menu-item">{{this.name}}</td>
                    <td class="admin__menu-item">
                        <a href="" data-id="{{this._id}}" class="btn-recovery">Khôi phục</a>
                    </td>
                    <td class="admin__menu-item">
                        <a href="" data-id="{{this._id}}" data-toggle="modal" data-target="#delete-user-modal">Xóa vĩnh viễn</a>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>

<!-- Modal -->
<div class="modal fade" id="delete-user-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Xác nhận xóa vĩnh viễn tài khoản?</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Sau khi xóa vĩnh viễn, tài khoản sẽ không thể khôi phục!!!! <br>
        Bạn có chắc chắn muốn xóa tài khoản này không?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Hủy</button>
        <button type="button" class="btn btn-danger" id="btn-delete-user" >Xóa</button>
      </div>
    </div>
  </div>
</div>

{{!-- delete hiden form --}}
<form id="delete-user-form" method="POST"></form>
<form id="recovery-user-form" method="POST"></form>


<script>
    document.addEventListener('DOMContentLoaded', function() {
        var userId;
        var btnDeleteUser = document.getElementById('btn-delete-user');
        var btnRecovery = $('.btn-recovery');
        var deleteForm = document.getElementById('delete-user-form');
        var recoveryForm = document.getElementById('recovery-user-form');

        //When the dialog confirm button is clicked
        $('#delete-user-modal').on('show.bs.modal', function(event) {
            var button = $(event.relatedTarget);
            userId = button.data('id');
            console.log(userId);  
        });

        btnDeleteUser.onclick = function() {
            deleteForm.action = 'destroy/' + userId + '?_method=DELETE';
            deleteForm.submit();
        }

        //recovery user
        btnRecovery.click(function() {
            event.preventDefault();
            var userId = $(this).data('id');
            recoveryForm.action = 'recovery/' + userId + '?_method=PATCH';
            recoveryForm.submit();
            console.log(userId);
        });
    });
</script>